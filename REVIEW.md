- P1: [src/agent/mod.rs, src/agent/queue.rs, src/plan/runner.rs, tests/tier_integration.rs] The shared-drain refactor still tightens multiple public library entrypoints to require `Send` (`agent::process_message`, `agent::process_message_with_turn_builder`, `agent::drain_queue`, `agent::drain_queue_with_stats_fresh_turns`, `plan::run_plan_step`, `plan::tick_plan_runner`). That is a real API regression for any caller using a single-threaded / `!Send` provider factory or approval handler, and the updated helper in `tests/tier_integration.rs` already shows the new restriction leaking outward.
- P2: [src/lib.rs, src/session_runtime/mod.rs, src/session_runtime/factory.rs] Making `session_runtime` public again reintroduces a new library surface just to let the binary reuse the factory helpers. That now exposes `autopoiesis::session_runtime::*` and its OpenAI-specific factory functions as supported API even though they are queue-plumbing internals and were previously intentionally hidden.
