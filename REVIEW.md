- P1: [src/app/session_run.rs resolve_session_id] The new registry fallback fabricates `"{active_agent}-t1"` whenever `active_agent` is set, even if the `t1` tier is not configured and the registry is empty. In that case a bare CLI run now silently creates and uses a non-registry `*-t1` session instead of erroring or selecting a real configured default.
- P2: [src/server/ws.rs websocket_always_on_prompt_drains_inline_while_worker_is_paused] The regression test still calls `handle_ws_prompt()` directly instead of exercising `websocket_session()` and the real websocket reader/writer lifecycle. That leaves the new connection-scoped count increment/decrement path untested, so disconnect cleanup and prompt routing through the actual websocket loop can still regress without this test failing.
