- P2: [scripts/pre_commit_secret_scan.sh] The test-only Rust path policy is still duplicated in Bash and Python, and the two implementations are not equivalent. For example, `src/agent/tests/nested/common.rs` matches the Bash short-circuit (`[[ "$path" == */tests/*.rs ]]`) but fails the Python helper (`re.search(r"/tests/[^/]+\.rs$", path)`). The current behavior works only because the Bash check happens to run first, which makes the exemption logic easy to drift again.
- P2: [src/server/queue.rs/tests] `fresh_turn_builder_matches_reference_turn_with_subscriptions` still compares `tool_definitions()` and `check_tool_call(...)` via `format!("{:?}", ...)`. That couples the regression to `Debug` output rather than behavior, so an innocuous formatting change will fail the test while a semantic regression can still slip through if the debug text stays the same. Assert concrete tool names / verdict properties instead.
- P2: [src/server/queue.rs/tests] The strengthened fresh-turn regression now proves provider-input parity, but it still stops at `drain_result.is_ok()`. It never asserts that the queued row ended in the expected terminal state or that the drain returned a processed assistant turn, so queue bookkeeping regressions can still pass this test unnoticed.
